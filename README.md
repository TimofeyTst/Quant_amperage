# Interface App for device
## Config
### Linux (Ubuntu) creating python venv
``` python3 -m venv venv
    source venv/bin/activate

    pip install django
    pip install visa
    pip install numpy
```
